# Learnings

- If you are adding elements to your html page but, nothing is visible then, add `z-index` with a big number this will put the element that's not visible to the very top also, add `position:relative` and odd colors for better visibility.
