# 📘 Technical Notes

## ❗ Pending Questions & Tasks

- **Theory (T) — Attempt 3:**
  - Types of streams
  - Router Chaining
  - Status Codes: 401, 301
  - Dynamic Routing
  - How to install a dev dependency
  - Session destroy syntax
  - `app.locals`

- **Theory (T) — Attempt 2 (Failed):**
  - `process.nextTick`
  - Buffer class
  - Concurrency and thread pool
  - Types of streams
  - Piping
  - OPTIONS method
  - HTTP in general
  - `PUT` vs `PATCH`
  - Middlewares and types
  - `fs` module practicals
  - `express.urlencoded()`
  - `express.json()`
  - Environment variables (`.env`)

---

# 📊 Marks Overview

## 🧠 Theory

- Score: **5.5 / 10**
- Cut-off: 5
- Status: ✅ Passed

## 🛠 Practical

- Score: **5 / 10**
- Cut-off: 5
- Status: ✅ Passed

## 🏆 Full Domain 1

- Completed on: **Jul 18, 2026 01:03 PM** (3rd attempt)
- Overall Score: **5.3 / 10**
- Status: ✅ Passed

> 1st attempt — Absent/Exam extended
> 2nd attempt — Failed (slacked off)

## 💬 Examiner Feedback

- Learn the concepts in detail — was missing depth in answers.
- Practice more problems.

---

# 🗣️ Communication Skills Breakdown

_Note: Detailed numerical scores were not provided._

### ✅ Feedback

- Communication is good.

---

# 🧠 Observations (Real Talk)

- **Barely cleared the line:** 5.5 theory and 5 practical are right at the cut-off — there's zero safety margin.
- **Third attempt to pass:** First two attempts were absent/failed. This module took real effort to get through, so the grind is real.
- **Theory gaps are wide:** Between both attempts, pendings span core Node (nextTick, Buffer, concurrency, streams, piping), HTTP (methods, OPTIONS, put vs patch), Express (middlewares, routing, status codes), and tooling (dev deps, sessions, app.locals) — that's 16 topics total.
- **Practical is the weakest link:** `fs` module, `express.urlencoded()`, `express.json()`, and `.env` handling all need hands-on drilling. 5/10 means half the code portion was shaky.
- **Communication held up:** "Good" feedback is consistent, which is a plus.

---

# 🚀 What You Should Do Next

- **Deep dive into Streams & Piping:** Understand Readable, Writable, Duplex, Transform, and how piping connects them.
- **Master `process.nextTick` & Concurrency:** Know the event loop phases, thread pool, and when `nextTick` fires vs `setImmediate`.
- **Learn Buffer Class:** Understand what Buffers are, how they handle binary data, and common methods.
- **Nail HTTP & Express:** Know all HTTP methods (especially OPTIONS), `PUT` vs `PATCH`, status codes (401 vs 403, 301 vs 302), and middleware types.
- **Hands-on with `fs`, `.env`, and Express body parsers:** Practice `fs` module file ops, `express.json()`, `express.urlencoded()`, and `dotenv` setup.
- **Push for 7+:** You're at the cut-off — aim higher to build a safety margin for future modules.
